# Database Configuration
DB_FILE_NAME=mydb.sqlite

# Session Configuration
# IMPORTANT: Generate a new secret key with: openssl rand -base64 32
# Never commit the real SESSION_SECRET to version control
SESSION_SECRET=your-secret-key-here

# Environment
# Set to 'production' in production environment for secure cookies
NODE_ENV=development

apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: asobiba
  labels:
    app.kubernetes.io/name: portal
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app: portal
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
